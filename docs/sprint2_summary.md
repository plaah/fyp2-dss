# Sprint 2 Summary — Apr 14–20, 2026

**Status:** Complete  
**Demo 1 Date:** Apr 20–23, 2026

---

## T2.1 — Model Training Results

| Model | Accuracy | Weighted F1 | AUC-ROC (OvR) |
|---|---|---|---|
| Random Forest (baseline) | 99.85% | 0.9985 | 1.0000 |
| **XGBoost (primary)** | **99.85%** | **0.9985** | **0.9996** |
| LightGBM (secondary) | 99.85% | 0.9985 | 0.9997 |

**Best model:** XGBoost (selected as primary target; RF tie-broken by plan preference)  
**Best iteration (early stopping):** 150 rounds  
**Target ≥85% accuracy:** Exceeded — 99.85%

### Classification Report (XGBoost, Test Set)
```
                   precision    recall  f1-score   support
coding_incomplete      0.992     1.000     0.996       389
 grouping_invalid      1.000     0.985     0.992       200
   grouping_valid      1.000     1.000     1.000      1389
         accuracy                          0.998      1978
```

### Notes
- All 3 models converged to identical accuracy — consistent with the synthetic data being generated by GaussianCopula (models can perfectly reconstruct the distribution)
- XGBoost + SHAP early-stopping compatibility issue resolved: retrained clean model at `best_iteration=150` on full training set

---

## T2.2 — API Endpoint Status

| Endpoint | Method | Status | Notes |
|---|---|---|---|
| `/api/v1/health` | GET | ✅ Working | Returns model_loaded, model_name, version |
| `/api/v1/predict` | POST | ✅ Working | Returns prediction + confidence + SHAP |
| `/api/v1/financial-impact` | POST | Stub | Sprint 3 |
| `/api/v1/feedback` | POST | Stub | Sprint 5 |

### Sample /predict Response
```json
{
  "prediction": "grouping_valid",
  "confidence": {
    "coding_incomplete": 0.0001,
    "grouping_invalid": 0.0,
    "grouping_valid": 0.9998
  },
  "explanation": [
    {"feature": "final_success", "impact": 5.0993, "direction": "positive"},
    {"feature": "idrg_primary_icd10", "impact": 0.032, "direction": "positive"},
    {"feature": "inacbg_primary_icd10", "impact": 0.0158, "direction": "positive"}
  ],
  "model_used": "XGBoost",
  "status": "success"
}
```

---

## T2.3 — SHAP Explainability

**Top 5 features (mean |SHAP|):**
1. `final_success` — whether iDRG + INACBG both succeeded
2. `claim_stage` — workflow stage of the claim
3. `inacbg_primary_icd10` — INACBG ICD-10 frequency (key clinical signal)
4. `entry_type` — patient entry type (outpatient/inpatient/emergency)
5. `claim_status` — overall claim status

**Plots saved:**
- `docs/shap_plots/shap_bar_plot.png` — mean |SHAP| bar chart (top 15 features)
- `docs/shap_plots/shap_summary_plot.png` — beeswarm summary plot

---

## Issues Encountered & Resolutions

| Issue | Fix |
|---|---|
| XGBoost 2.0 + SHAP `BoostedRounds` mismatch when using early stopping | Retrained final model at `best_iteration` on full train set without early stopping |
| Flask port 5000 conflict (macOS AirPlay) | Used port 5001 for testing; app.py runs on 5000 by default |
| SDV synthetic artifacts in `claim_stage`/`entry_type` (~999 unique values) | Handled via frequency encoding in pipeline.py |

---

## Files Produced

```
models/
  best_model.pkl          ← XGBoost (150 iterations, clean)
  xgboost_model.pkl
  lightgbm_model.pkl
  rf_baseline.pkl
  label_encoder.pkl
  preprocessing.pkl       ← LabelEncoders + freq maps for predictor
  feature_names.txt

src/services/
  predictor.py            ← preprocess_input() + predict()
  explainer.py            ← SHAP TreeExplainer + plot generation

src/api/routes.py         ← /health + /predict (live)

docs/
  model_training_results.md
  sprint2_summary.md
  shap_plots/
    shap_bar_plot.png
    shap_summary_plot.png

scripts/sprint2_status.py ← context recovery script
notebooks/03_model_training.py
```
